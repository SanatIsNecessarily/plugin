1、插件正式发布后 下线（逻辑删除）  可以重新上线（如果是版本下线，则可以版本上线， 如果是插件下线，那么插件上线是否还原下线之前各个版本的状态）？
2、插件可以同时进行多个灰度版本，但灰度转正式的时候，是否要控制当前灰度转正式的版本要高于当前最新正式版本

3、是否要提供插件的物理删除，后续可以重新上传、
4、用户插件列表应该如何展示？ 有个提案（新增一个页签，专门用来查询用户所有提交待审核的插件）另外一个页签提供用户所拥有的每个插件的最新版本
5、管理员界面，针对待审核数据，单独新增一个页签，另外一个页签展示所有插件，在此页签下，可以对插件进行强制下线操作（发送邮件）
6、所有对插件进行生命周期的情况，都需要发送邮件？

7、用户撤销灰度（是直接删除数据并将整个灰度版本的数据也删除吗？）
8、